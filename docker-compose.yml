services:
  bipolar-code:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - bipolar-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  bipolar-data:
